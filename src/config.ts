import configData from '../ism.config.json'

export const models: string[] = configData.models
export const modes: string[] = configData.modes
export const defaultMappingModeBy: string = configData.defaultMappingModeBy

export const version: string = configData.version
